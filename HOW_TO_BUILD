Run build.bat, if on a UNIX system, then create shell script that accomplishes the exact task. 
To test OS, copy img.bin into the first few sectors of a blank VHD and run VirtualBox and boot from VHD. Or using low level disk acess copy the img.bin into a USB
stick. 


Note: MonsterOS was designed to boot off of the hardisk and not a CD image or CD.